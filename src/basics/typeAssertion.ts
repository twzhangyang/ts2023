const load = () => "";

const hello2 = load();

const trimmed = (hello as string).trim();


let leet;
leet = '1233';

// Use as number
// It will have a compile error
// const number = leet as number;
// console.log(number == 1233); //false
